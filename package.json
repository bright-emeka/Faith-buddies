{
  "name": "bible-social-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Faith Buddies",
  "scripts": {
    "web:install": "npm --prefix backend install && npm --prefix frontend install",
    "web:dev": "concurrently --kill-others \"npm --prefix backend run dev\" \"vite frontend\"",
    "web:build": "vite build frontend",
    "web:serve": "npm --prefix frontend run build && npm --prefix backend start",
    "web:start": "npm --prefix backend start",
    "heroku-postbuild": "npm run web:install && npm run web:build"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.1",
    "concurrently": "^8.2.2",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "cli": "^1.0.1",
    "firebase": "^12.13.0",
    "render": "^0.1.4"
  }
}
